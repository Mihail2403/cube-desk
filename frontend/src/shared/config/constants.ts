/** Максимум вложений за один запрос сообщения (сервер). */
export const MAX_ATTACHMENT_FILES = 10;

/** Максимальный размер одного файла (байты), ~10 MiB. */
export const MAX_ATTACHMENT_BYTES = 10 * 1024 * 1024;

export const DEFAULT_TICKETS_PAGE_SIZE = 50;

export const DEFAULT_MESSAGES_LIMIT = 200;
